import { usersArray } from "../data";
import User from "./user";

// export class allUsers {
//     findUser(id:number){
//         const user = usersArray.find((f) => f.id === id);
//         if (!user){
//             return 
//         }
//         return user
//     }
    
//       finIdTransaction(userId:number,transId:number) {
//         const findTransition = this.findUser(userId);
//         findTransition.transactions.find( (f) => f.id === transId);
//         return findTransition;
//       }
// }